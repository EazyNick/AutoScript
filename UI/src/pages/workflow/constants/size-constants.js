/**
 * 크기 및 거리 관련 상수
 * 캔버스 크기, 임계값, 마진 등을 정의합니다.
 */

export const SIZE_CONSTANTS = {
    // 무한 캔버스 크기
    INFINITE_CANVAS_WIDTH: 50000,
    INFINITE_CANVAS_HEIGHT: 50000,

    // 연결 및 상호작용
    MAGNETIC_THRESHOLD: 40, // 마그네틱 연결 거리 임계값 (px)
    MOUSE_POSITION_ADJUSTMENT: 10, // 마우스 위치 보정 값 (px)

    // 스타일 관련
    STROKE_WIDTH: 3, // 연결선 두께 (px)
    CONNECTOR_SIZE: 10 // 커넥터 크기 (px)
};
